{
  "name": "jev-cloud-cost-guardian",
  "version": "0.8.0",
  "description": "Evaluate cloud spend against a budget and gate CI with Jev (approve, warn, block, or review).",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "catalog:from-infracost": "node scripts/generate-pricing-catalog.mjs",
    "all": "npm run typecheck && npm run test:coverage && npm run build"
  },
  "keywords": [
    "github-actions",
    "jev",
    "typesafe",
    "finops",
    "cloud-cost",
    "budget",
    "ci-cd",
    "jevforge"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/JevForge/jev-cloud-cost-guardian.git"
  },
  "bugs": {
    "url": "https://github.com/JevForge/jev-cloud-cost-guardian/issues"
  },
  "homepage": "https://github.com/JevForge/jev-cloud-cost-guardian#readme",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1",
    "@aws-sdk/client-cost-explorer": "^3.901.0",
    "ai": "^7.0.113",
    "yaml": "^2.8.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vitest/coverage-v8": "^3.0.9",
    "esbuild": "^0.25.12",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.28.2": true
  }
}
